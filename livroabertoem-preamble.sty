\RequirePackage{tikz}
\def\input@path{%
  {figures}
}

\input{definitions/la-lists.tex}
\input{definitions/la-tables.tex}
\input{definitions/la-credits.tex}

\RequirePackage{definitions/la-tikzconfig}
\RequirePackage{float}
\RequirePackage{caption}
\RequirePackage{microtype}

\RequirePackage{siunitx}
\RequirePackage{currency}
\newcommand{\reais}{R\$}
\DefineCurrency{USD}{name = {dólar}, plural = {dólares}, iso = {USD}, kind = symbol, symbol = {\$}}
\DefineCurrency{BRL}{name = {real}, plural = {reais}, iso = {BRL}, kind = symbol, symbol = {\reais}}
\DefineCurrency{EUR}{name={euro}, plural={euros}, symbol={\euro}, iso={EUR}, kind=symbol}
\CurrencySetup{pre = true, group-separator = {.}, base = {2}}

\sisetup{
	per-mode = symbol,
	group-separator = {.},
	output-decimal-marker = {,},
	range-phrase = { a },
	range-units = single,
	detect-mode,
	retain-zero-uncertainty,
	drop-zero-decimal=false
}

\counterwithin{figure}{chapter}
\renewcommand{\fps@figure}{!htb}
\renewcommand{\fps@table}{!htb}
\g@addto@macro\@floatboxreset\centering

\RequirePackage[brazilian]{babel}
\babelprovide[transforms = hyphen.repeat]{brazilian}
\addto\captionsbrazilian{%
  \renewcommand{\appendixname}{Apêndice}%
  \renewcommand{\bibname}{Bibliografia}
}

\RequirePackage[
	style = apa,
	backend = biber
]{biblatex}
\RequirePackage[portuguese=brazilian]{csquotes}


\input{definitions/la-math.tex}

\input{definitions/la-hypersetup.tex}

